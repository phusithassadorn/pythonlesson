# from math import * (* is import all function )
from math import floor,ceil,sqrt # import a bunch of different math functions to use in this file

print(4)
print(6 - 1)
print(15 + 23 )
print(12 / 45 * 24 - 75 + 3)

my_num = 7
print(str(my_num) + " my favorit number") # convert number type to text type

my_num_2 = -5
print(abs(my_num_2)) # show absolute number (ค่าสัมบูรณ์)
print(pow(4,6)) # show power number (ค่ายกกำลัง)

print(max(1,2,3,4,6,5,6,7,8,9)) # show most numbers
print(min(1,2,3,4,6,5,6,7,8,9)) # show the smallest number

print(round(3.7)) # round number up in this case 3.7 to 4 (ปัดค่าตัวเลข) because this more then .5
print(round(3.4)) # round number down in this case 3.4 to 3 (ปัดค่าตัวเลข) because this low then .5

print(floor(3.45)) # round number up if have a .00 don't care what ever number is
print(ceil(3.45)) # round number down if have a .00 don't care what ever number is

print(sqrt(36)) # return the square root of a number 